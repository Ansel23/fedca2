


const typed = new Typed('.auto-input',{
    strings:["I am Ansel","and welcome to my page"],
    typeSpeed:80,  // Note: "typeSpeed" should be in camelCase, not "typespeed"
    backSpeed:40,
    backDelay:1000,  // Note: "backSpeed" should be in camelCase, not "backspeed"
    loop:true
  });